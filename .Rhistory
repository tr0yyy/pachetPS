f<-function (x) { return (exp(-x^2/2)); }
Media <- function(functie){
medie=tryCatch({
return(integrate(x*functie(x), -Inf, Inf)$value)
}, eroare = function(e) {
warning(c("Nu se poate calcula media variabilei aleatoare continue"));
return(0);
})
return(medie)
}
Media(f)
View(Media)
